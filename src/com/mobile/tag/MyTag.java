package com.mobile.tag;import com.mobile.util.DaoFactory;import javax.servlet.jsp.JspWriter;import javax.servlet.jsp.tagext.SimpleTagSupport;import java.io.IOException;public class MyTag extends SimpleTagSupport {    public static  String[] AREAS = DaoFactory.getSeatDao().getSeatAreas();//动态获取坐席所在的区域    private String area;    public String getArea() {        return area;    }    public void setArea(String area) {        this.area = area;    }    @Override    public void doTag() throws IOException {        JspWriter out = getJspContext().getOut();        StringBuilder str = new StringBuilder();        str.append("<select class=\"input\" name=\"area\" id=\"area\" data-validate=\"required:请选择坐席位置\">");        str.append("<option value=\"\">请选择坐席位置</option>");        for (String s : AREAS) {            if (s.equals(area)) {                str.append("<option value='").append(s).append("' selected>").append(s).append("</option>");            } else {                str.append("<option value='").append(s).append("'>").append(s).append("</option>");            }        }        str.append("</select>");        out.print(str.toString());    }}