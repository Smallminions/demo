package com.mobile.servlet.employee;import com.mobile.util.DaoFactory;import javax.servlet.ServletException;import javax.servlet.annotation.WebServlet;import javax.servlet.http.HttpServlet;import javax.servlet.http.HttpServletRequest;import javax.servlet.http.HttpServletResponse;import java.io.IOException;import java.io.PrintWriter;@WebServlet(name = "CheckIdServlet", urlPatterns = {"/CheckIdServlet"})public class CheckIdServlet extends HttpServlet {    protected void doPost(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {    }    protected void doGet(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {        response.setContentType("text/html;charset=UTF-8");        PrintWriter out = response.getWriter();        String eid = request.getParameter("id");//登录id        String aid = request.getParameter("aid");//添加员工的id        if(aid==null) {            try {                if (DaoFactory.getEmployeeDao().findEmployee(eid)) {                    //账号存在                    out.println("{\"getdata\":\"true\"}");                } else {                    //账号不存在                    out.println("{\"getdata\":\"false\"}");                }            } finally {                out.close();            }        }else {            try {                if (DaoFactory.getEmployeeDao().findEmployee(aid)) {                    System.out.println(aid);                    //账号存在                    out.println("{\"getdata\":\"false\"}");                } else {                    //账号不存在                    out.println("{\"getdata\":\"true\"}");                }            } finally {                out.close();            }        }    }}