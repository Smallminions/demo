package com.mobile.util;import java.sql.*;public class DatabaseBean {    public static Connection getConnection(){        Connection con = null;          //连接        Statement sta = null;        PreparedStatement pre = null;        ResultSet res = null;           //结果集        String  Url = "jdbc:mysql://localhost/minions?seUnicode=true&useSSL=false&characterEncoding=utf8";        String username = "wb";        String password = "minions";        try {            //加载驱动            Class.forName("com.mysql.jdbc.Driver");            con = DriverManager.getConnection(Url, username, password);        } catch (ClassNotFoundException e){            e.printStackTrace();        }catch (SQLException e){            e.printStackTrace();        }        return con;    }    public static void close(ResultSet re,Statement st,Connection con){        try {            if (re != null) {                re.close();            }            if (st != null) {                st.close();            }            if (con != null) {                con.close();            }        }catch(SQLException e){            e.printStackTrace();        }    }}